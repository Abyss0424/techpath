@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Fira+Code:wght@300;400;500;600&family=Space+Grotesk:wght@400;500;600;700&display=swap');

/* ═══════════════════════════════════════════════════════════════════════════
   TECHPATH · CYBER-PRECISION DESIGN SYSTEM · v2.0
   "NSA analyst tool designed by Apple"
   ═══════════════════════════════════════════════════════════════════════════ */

:root {
  /* ── BASE SURFACES (THE VOID) ─────────────────────────────────────────── */
  --bg: #04080F;
  --bg-secondary: #0e141b;
  --bg-panel: #171c24;
  --bg-elevated: #252a32;
  --bg-card: #1b2028;
  --bg-hi: #30353d;

  /* ── TACTICAL ACCENTS ────────────────────────────────────────────────── */
  --cyan: #00F2FE;
  --cyan-dim: rgba(0, 242, 254, 0.7);
  --cyan-glow: rgba(0, 242, 254, 0.2);
  --cyan-glass: rgba(0, 242, 254, 0.06);
  --green: #4EEE94;
  --green-dim: rgba(78, 238, 148, 0.7);
  --green-glow: rgba(78, 238, 148, 0.2);
  --green-glass: rgba(78, 238, 148, 0.06);

  /* ── PATH-SPECIFIC (COLD TECH) ───────────────────────────────────────── */
  --red: #FF4444;
  --orange: #FF9500;
  --purple: #BB44FF;
  --gold: #FFD700;
  --teal: #00FFB3;
  --blue-path: #00A8FF;

  /* ── TEXT (DESATURATED RIGOR) ────────────────────────────────────────── */
  --text: #dee2ed;
  --text-muted: #b9cacb;
  --text-mid: #dee2ed;
  --text-h: var(--text);

  /* ── BORDERS (GHOST ETCHING) ─────────────────────────────────────────── */
  --border: rgba(0, 242, 254, 0.12);
  --border-hi: rgba(0, 242, 254, 0.18);
  --border-green: rgba(78, 238, 148, 0.18);

  /* ── TYPOGRAPHY ──────────────────────────────────────────────────────── */
  --sans: 'Inter', system-ui, sans-serif;
  --heading: 'Space Grotesk', sans-serif;
  --mono: 'Fira Code', monospace;

  /* ── SPACING (LINEAR SCALE) ──────────────────────────────────────────── */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 24px;
  --space-6: 32px;
  --space-7: 48px;
  --space-8: 64px;
  --space-9: 96px;
  --space-10: 128px;

  /* ── BORDER RADIUS (TACTICAL PRECISION) ──────────────────────────────── */
  --radius-sm: 2px;
  --radius-md: 4px;
  --radius-lg: 8px;
  --radius-pill: 100px;

  /* ── Z-INDEX ─────────────────────────────────────────────────────────── */
  --z-background: -1;
  --z-content: 1;
  --z-sticky: 100;
  --z-modal: 1000;
  --z-cursor: 9999;

  /* ── GLOW SYSTEM (TACTICAL HUD) ─────────────────────────────────────── */
  --shadow-cyan-sm: 0 0 12px rgba(0, 242, 254, 0.25);
  --shadow-cyan-md: 0 0 20px rgba(0, 242, 254, 0.3), 0 0 40px rgba(0, 242, 254, 0.1);
  --shadow-green-sm: 0 0 12px rgba(78, 238, 148, 0.25);
  --shadow-green-md: 0 0 20px rgba(78, 238, 148, 0.3), 0 0 40px rgba(78, 238, 148, 0.1);

  font: 16px/1.5 var(--sans);
  letter-spacing: -0.01em;
  color-scheme: dark;
  color: var(--text);
  background: var(--bg);
  font-synthesis: none;
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* ═══════════════════════════════════════════════════════════════════════════
   RESET
   ═══════════════════════════════════════════════════════════════════════════ */
* {
  box-sizing: border-box;
}

body {
  margin: 0;
  background: var(--bg);
}

#root {
  width: 100%;
  max-width: 100%;
  min-height: 100svh;
  margin: 0;
  text-align: left;
  border: none;
  display: flex;
  flex-direction: column;
}

h1,
h2 {
  font-family: var(--heading);
  font-weight: 600;
  color: var(--text-h);
  margin: 0;
}

p {
  margin: 0;
}

code {
  font-family: var(--mono);
  font-size: 13px;
  line-height: 1.4;
  padding: 2px 6px;
  background: var(--cyan-glass);
  color: var(--cyan);
  border-radius: var(--radius-sm);
}

/* ═══════════════════════════════════════════════════════════════════════════
   SCROLLBARS
   ═══════════════════════════════════════════════════════════════════════════ */
::-webkit-scrollbar {
  width: 4px;
}

::-webkit-scrollbar-track {
  background: transparent;
}

::-webkit-scrollbar-thumb {
  background: rgba(0, 242, 254, 0.2);
  border-radius: 2px;
}

::-webkit-scrollbar-thumb:hover {
  background: rgba(0, 242, 254, 0.4);
}

/* ═══════════════════════════════════════════════════════════════════════════
   KEYFRAMES
   ═══════════════════════════════════════════════════════════════════════════ */

/* ── Core utilities ──────────────────────────────────────────────────────── */
@keyframes pulse {

  0%,
  100% {
    opacity: 1;
  }

  50% {
    opacity: 0.3;
  }
}

@keyframes blink {

  0%,
  100% {
    opacity: 1;
  }

  50% {
    opacity: 0;
  }
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }

  to {
    opacity: 1;
  }
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slideInLeft {
  from {
    transform: translateX(-100%);
  }

  to {
    transform: translateX(0);
  }
}

@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-20px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* ── Glow effects ────────────────────────────────────────────────────────── */
@keyframes glow-pulse {

  0%,
  100% {
    box-shadow: 0 0 8px rgba(0, 242, 254, 0.3);
  }

  50% {
    box-shadow: 0 0 20px rgba(0, 242, 254, 0.6);
  }
}

@keyframes pulse-green {

  0%,
  100% {
    box-shadow: 0 0 10px rgba(78, 238, 148, 0.4);
    transform: scale(1);
  }

  50% {
    box-shadow: 0 0 25px rgba(78, 238, 148, 0.8);
    transform: scale(1.02);
  }
}

@keyframes dot-pulse {

  0%,
  100% {
    opacity: 1;
    transform: scale(1);
    box-shadow: 0 0 0 0 rgba(78, 238, 148, 0.5);
  }

  50% {
    opacity: 0.7;
    transform: scale(1.2);
    box-shadow: 0 0 0 4px rgba(78, 238, 148, 0);
  }
}

/* ── Background effects ──────────────────────────────────────────────────── */
@keyframes grid-move-y {
  0% {
    transform: translateY(0);
  }

  100% {
    transform: translateY(50px);
  }
}

@keyframes grid-move-x {
  0% {
    transform: translateX(0);
  }

  100% {
    transform: translateX(50px);
  }
}

@keyframes float-orb {

  0%,
  100% {
    transform: translate(0, 0) scale(1);
    border-radius: 60% 40% 70% 30% / 50% 60% 40% 50%;
  }

  33% {
    transform: translate(30px, -20px) scale(1.05);
    border-radius: 40% 60% 30% 70% / 60% 40% 70% 30%;
  }

  66% {
    transform: translate(-20px, 15px) scale(0.97);
    border-radius: 70% 30% 50% 50% / 30% 60% 40% 70%;
  }
}

@keyframes scan-line {
  0% {
    transform: translateY(-100vh);
    opacity: 0;
  }

  5% {
    opacity: 1;
  }

  95% {
    opacity: 1;
  }

  100% {
    transform: translateY(100vh);
    opacity: 0;
  }
}

@keyframes data-pulse {

  0%,
  100% {
    opacity: 0.03;
  }

  50% {
    opacity: 0.12;
  }
}

/* ── Component effects ───────────────────────────────────────────────────── */
@keyframes float-mockup {

  0%,
  100% {
    transform: perspective(1000px) rotateX(10deg) rotateY(-15deg) rotateZ(2deg) translateY(0px);
  }

  50% {
    transform: perspective(1000px) rotateX(10deg) rotateY(-15deg) rotateZ(2deg) translateY(-12px);
  }
}

@keyframes marquee {
  0% {
    transform: translateX(0);
  }

  100% {
    transform: translateX(-50%);
  }
}

@keyframes glitch {
  0% {
    text-shadow: none;
    transform: none;
  }

  20% {
    text-shadow: -2px 0 var(--red), 2px 0 var(--cyan);
    transform: translate(1px, 0);
  }

  40% {
    text-shadow: 2px 0 var(--red), -2px 0 var(--cyan);
    transform: translate(-1px, 0);
  }

  60% {
    text-shadow: -1px 0 var(--green);
    transform: translate(0, 0);
  }

  80% {
    text-shadow: 1px 0 var(--cyan);
    transform: translate(1px, 0);
  }

  100% {
    text-shadow: none;
    transform: none;
  }
}

@keyframes scanline-pass {
  0% {
    top: -2px;
  }

  100% {
    top: 100%;
  }
}

@keyframes typewriter-cursor {

  0%,
  100% {
    opacity: 1;
  }

  50% {
    opacity: 0;
  }
}

@keyframes particle-burst {
  0% {
    opacity: 1;
    transform: translate(0, 0) scale(1);
  }

  100% {
    opacity: 0;
    transform: var(--burst-dir, translate(20px, -20px)) scale(0);
  }
}

@keyframes counter-up {
  from {
    opacity: 0;
    transform: translateY(10px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes bar-fill {
  from {
    width: 0%;
  }

  to {
    width: var(--bar-target, 60%);
  }
}

@keyframes progress-bar-shimmer {
  0% {
    background-position: -200px 0;
  }

  100% {
    background-position: 200px 0;
  }
}

@keyframes session-blink {

  0%,
  100% {
    opacity: 1;
  }

  50% {
    opacity: 0.3;
  }
}

/* ═══════════════════════════════════════════════════════════════════════════
   UTILITY CLASSES
   ═══════════════════════════════════════════════════════════════════════════ */

/* ── Background layers ───────────────────────────────────────────────────── */
.cyber-grid-lg {
  position: absolute;
  inset: 0;
  background-image:
    linear-gradient(rgba(0, 242, 254, 0.04) 1px, transparent 1px),
    linear-gradient(90deg, rgba(0, 242, 254, 0.04) 1px, transparent 1px);
  background-size: 100px 100px;
  animation: grid-move-y 20s linear infinite;
  pointer-events: none;
}

.cyber-grid-sm {
  position: absolute;
  inset: 0;
  background-image:
    linear-gradient(rgba(0, 242, 254, 0.02) 1px, transparent 1px),
    linear-gradient(90deg, rgba(0, 242, 254, 0.02) 1px, transparent 1px);
  background-size: 20px 20px;
  animation: grid-move-x 15s linear infinite;
  pointer-events: none;
}

/* ── Glass effect ────────────────────────────────────────────────────────── */
.glass-card {
  background: rgba(14, 20, 28, 0.6);
  backdrop-filter: blur(12px);
  border: 1px solid var(--border);
  box-shadow: var(--shadow-card);
}

.glass-panel {
  background: rgba(9, 14, 22, 0.95);
  backdrop-filter: blur(16px);
  border: 1px solid rgba(0, 242, 254, 0.2);
  box-shadow: 0 0 0 1px rgba(0, 242, 254, 0.05), 0 32px 64px rgba(0, 0, 0, 0.8), 0 0 80px rgba(0, 242, 254, 0.05);
}

/* ── Neon text ───────────────────────────────────────────────────────────── */
.neon-cyan {
  color: var(--cyan);
  text-shadow: 0 0 10px rgba(0, 242, 254, 0.5);
}

.neon-green {
  color: var(--green);
  text-shadow: 0 0 10px rgba(78, 238, 148, 0.5);
}

/* ── Corner brackets decoration ─────────────────────────────────────────── */
.corner-brackets {
  position: relative;
}

.corner-brackets::before,
.corner-brackets::after {
  content: '';
  position: absolute;
  width: 12px;
  height: 12px;
  border-color: rgba(0, 242, 254, 0.3);
  border-style: solid;
  transition: border-color 0.3s;
}

.corner-brackets::before {
  top: -1px;
  left: -1px;
  border-width: 2px 0 0 2px;
}

.corner-brackets::after {
  bottom: -1px;
  right: -1px;
  border-width: 0 2px 2px 0;
}

.corner-brackets:hover::before,
.corner-brackets:hover::after {
  border-color: rgba(0, 242, 254, 0.7);
}

/* ── Scan-line overlay ───────────────────────────────────────────────────── */
.scan-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 2px;
  background: linear-gradient(90deg, transparent, rgba(0, 242, 254, 0.4), transparent);
  animation: scan-line 8s linear infinite;
  pointer-events: none;
  z-index: 50;
}

/* ── Eyebrow tag ─────────────────────────────────────────────────────────── */
/* ── Eyebrow tag ─────────────────────────────────────────────────────────── */
.eyebrow-tag {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-family: var(--mono);
  font-size: 10px;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  color: var(--cyan);
  background: rgba(0, 242, 254, 0.04);
  border: 1px solid var(--border-hi);
  padding: 6px 12px;
  border-radius: var(--radius-sm);
  margin-bottom: var(--space-5);
}

.dot-pulse {
  width: 6px;
  height: 6px;
  background: var(--green);
  border-radius: 50%;
  animation: dot-pulse 2s ease-in-out infinite;
  box-shadow: 0 0 8px var(--green-glow);
}

/* ── Hero Layout ─────────────────────────────────────────────────────────── */
.hero-section {
  position: relative;
  min-height: calc(100vh - 80px);
  display: flex;
  align-items: center;
  padding: var(--space-10) 5%;
  overflow: hidden;
  gap: var(--space-8);
}

.hero-content {
  flex: 1.1;
  max-width: 680px;
  z-index: 10;
}

.hero-visual {
  flex: 0.9;
  display: flex;
  justify-content: center;
  align-items: center;
  perspective: 1500px;
  position: relative;
}

.hero-title {
  font-size: clamp(3rem, 6vw, 5.2rem);
  line-height: 1;
  margin-bottom: var(--space-5);
  letter-spacing: -0.04em;
  text-transform: uppercase;
  color: var(--text);
}

.text-cyan { color: var(--cyan); text-shadow: 0 0 15px var(--cyan-glow); }
.text-green { color: var(--green); text-shadow: 0 0 15px var(--green-glow); }

.hero-desc {
  font-size: 1.1rem;
  color: var(--text-muted);
  margin-bottom: var(--space-7);
  max-width: 520px;
  line-height: 1.6;
}

.hero-ctas {
  display: flex;
  gap: var(--space-4);
  align-items: center;
  margin-bottom: var(--space-9);
}

/* ── Isometric Mockup ────────────────────────────────────────────────────── */
.isometric-mockup {
  position: relative;
  transform-style: preserve-3d;
  width: 480px;
  height: 320px;
  transition: transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
}

.hero-visual:hover .isometric-mockup {
  transform: rotateX(10deg) rotateY(-10deg) rotateZ(1deg);
}

.mockup-panel {
  position: absolute;
  background: rgba(14, 20, 28, 0.85);
  backdrop-filter: blur(20px);
  border: 1px solid var(--border-hi);
  border-radius: var(--radius-md);
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5), 0 0 40px var(--cyan-glass);
  padding: var(--space-4);
  transition: all 0.4s ease;
}

.panel-main {
  inset: 0;
  z-index: 2;
}

.panel-side {
  width: 140px;
  height: 240px;
  top: 40px;
  left: -60px;
  transform: translateZ(-50px);
  z-index: 1;
  opacity: 0.8;
}

.panel-intel {
  width: 180px;
  height: 120px;
  bottom: -40px;
  right: -40px;
  transform: translateZ(50px);
  z-index: 3;
  border-color: var(--border-green);
}

.mockup-header {
  display: flex;
  justify-content: space-between;
  margin-bottom: var(--space-4);
  font-family: var(--mono);
  font-size: 10px;
  color: var(--cyan);
  opacity: 0.6;
}

.mockup-line {
  height: 6px;
  background: var(--bg-hi);
  border-radius: 3px;
  margin-bottom: var(--space-3);
  width: var(--line-w, 100%);
}

.mockup-bar {
  height: 4px;
  background: var(--bg-hi);
  border-radius: 2px;
  overflow: hidden;
  margin-top: var(--space-4);
}

.mockup-fill {
  height: 100%;
  background: var(--green);
  width: 65%;
  box-shadow: 0 0 10px var(--green-glow);
}

.mockup-cursor {
  width: 2px;
  height: 14px;
  background: var(--cyan);
  display: inline-block;
  animation: blink 1s step-end infinite;
}


/* ── CTA primary button ──────────────────────────────────────────────────── */
.btn-primary {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  background: linear-gradient(135deg, var(--green) 0%, var(--cyan) 100%);
  color: #04080F;
  font-family: var(--heading);
  font-size: 15px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 1.5px;
  padding: 16px 36px;
  border: none;
  border-radius: var(--radius-sm);
  cursor: pointer;
  position: relative;
  overflow: hidden;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  box-shadow: 0 0 0px rgba(78, 238, 148, 0);
}

.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 0 30px rgba(78, 238, 148, 0.5), 0 0 60px rgba(0, 242, 254, 0.2);
}

.btn-primary:hover .btn-icon {
  transform: rotate(5deg) scale(1.15);
}

.btn-icon {
  transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  display: inline-block;
}

/* ── CTA ghost button ────────────────────────────────────────────────────── */
.btn-ghost {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: transparent;
  color: rgba(0, 242, 254, 0.8);
  font-family: var(--mono);
  font-size: 14px;
  border: 1px solid rgba(0, 242, 254, 0.3);
  padding: 16px 28px;
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: all 0.25s ease;
}

.btn-ghost:hover {
  background: rgba(0, 242, 254, 0.05);
  border-color: rgba(0, 242, 254, 0.8);
  color: var(--cyan);
}

.btn-ghost:hover .btn-arrow {
  transform: translateX(8px);
}

.btn-arrow {
  transition: transform 0.25s ease;
  display: inline-block;
}

/* ── Tech stack pills ────────────────────────────────────────────────────── */
.stack-pill {
  font-family: var(--mono);
  font-size: 11px;
  color: rgba(255, 255, 255, 0.6);
  background: rgba(0, 242, 254, 0.06);
  border: 1px solid rgba(0, 242, 254, 0.15);
  border-radius: var(--radius-sm);
  padding: 3px 8px;
  transition: all 0.2s ease;
  cursor: default;
  white-space: nowrap;
}

.stack-pill:hover {
  border-color: rgba(0, 242, 254, 0.5);
  color: #fff;
}

/* ── System Features ─────────────────────────────────────────────────────── */
.features-section {
  background: var(--bg-secondary);
  padding: var(--space-10) 5%;
  border-top: 1px solid rgba(0, 242, 254, 0.05);
}

.feature-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--space-6);
  max-width: 1200px;
  margin: 0 auto;
}

@media (max-width: 768px) {
  .feature-grid { grid-template-columns: 1fr; }
}

.feature-card {
  position: relative;
  background: var(--bg-card);
  padding: var(--space-7);
  border: 1px solid var(--border);
  min-height: 240px;
  transition: all 0.3s ease;
}

.feature-card:hover {
  background: var(--bg-hi);
  border-color: var(--cyan-dim);
  transform: translateY(-4px);
}

.ghost-number {
  position: absolute;
  top: var(--space-4);
  right: var(--space-5);
  font-family: var(--mono);
  font-size: 42px;
  font-weight: 700;
  color: var(--cyan);
  opacity: 0.05;
  transition: opacity 0.3s ease;
}

.feature-card:hover .ghost-number {
  opacity: 0.12;
}

.feature-tag {
  font-family: var(--mono);
  font-size: 10px;
  color: var(--cyan);
  background: var(--cyan-glass);
  padding: 2px 8px;
  border-radius: 2px;
  display: inline-block;
  margin-bottom: var(--space-4);
}

/* ── Operational Nodes ────────────────────────────────────�/* ── Dashboard (Command Center) ──────────────────────────────────────────── */
.dashboard-container {
  display: flex;
  height: 100vh;
  background: var(--bg);
  overflow: hidden;
  position: relative;
}

.panel-sidebar {
  width: 280px;
  flex-shrink: 0;
  background: var(--bg-secondary);
  border-right: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  z-index: 20;
}

.panel-feed {
  flex: 1;
  display: flex;
  flex-direction: column;
  background: var(--bg);
  position: relative;
  min-width: 0; /* Important for flex child with overflow */
}

.panel-intel {
  width: 320px;
  flex-shrink: 0;
  background: var(--bg-secondary);
  border-left: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  padding: var(--space-6);
  z-index: 10;
}

@media (max-width: 1200px) {
  .panel-intel { display: none; }
}

@media (max-width: 768px) {
  .panel-sidebar { 
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    transform: translateX(-100%);
    transition: transform 0.3s ease;
  }
  .panel-sidebar.open { transform: translateX(0); }
}

/* ── Messages (Tactical Blocks) ───────────────────────────────────────────── */
.message-user {
  align-self: flex-end;
  max-width: 85%;
  background: rgba(78, 238, 148, 0.04);
  border: 1px solid var(--border-green);
  border-right: 3px solid var(--green);
  padding: var(--space-4) var(--space-5);
  margin-bottom: var(--space-5);
  border-radius: var(--radius-sm);
}

.message-ai {
  align-self: flex-start;
  max-width: 90%;
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-left: 3px solid var(--cyan);
  padding: var(--space-5);
  margin-bottom: var(--space-6);
  border-radius: var(--radius-sm);
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
}

.message-label {
  font-family: var(--mono);
  font-size: 10px;
  color: var(--text-muted);
  margin-bottom: var(--space-3);
  letter-spacing: 1.5px;
  display: flex;
  justify-content: space-between;
}

/* ── Telemetry Widgets ───────────────────────────────────────────────────── */
.telemetry-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  padding: var(--space-5);
  margin-bottom: var(--space-5);
  position: relative;
}

.telemetry-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 10px;
  height: 10px;
  border-top: 2px solid var(--cyan);
  border-left: 2px solid var(--cyan);
}

.telemetry-header {
  font-family: var(--mono);
  font-size: 10px;
  color: var(--cyan);
  margin-bottom: var(--space-4);
  opacity: 0.8;
}

.telemetry-data {
  font-family: var(--mono);
  font-size: 16px;
  color: #fff;
  font-weight: 700;
}

/* ── Input Tactical Area ─────────────────────────────────────────────────── */
.input-area {
  padding: var(--space-5) var(--space-6);
  background: rgba(4, 8, 15, 0.9);
  backdrop-filter: blur(12px);
  border-top: 1px solid var(--border);
}

.input-container {
  display: flex;
  gap: var(--space-3);
  background: rgba(0, 0, 0, 0.4);
  border: 1px solid var(--border-hi);
  padding: var(--space-2);
  border-radius: var(--radius-sm);
}

.input-container input {
  flex: 1;
  background: transparent;
  border: none;
  color: #fff;
  padding: var(--space-3);
  font-family: var(--sans);
  font-size: 14px;
}

.input-container input:focus { outline: none; }
0, 0, 0, 0.8), 0 0 40px var(--cyan-glass);
  width: 100%;
  max-width: 680px;
  overflow: hidden;
  animation: modal-enter 0.5s cubic-bezier(0.16, 1, 0.3, 1);
  position: relative;
  z-index: 1;
}

@keyframes modal-enter {
  from { opacity: 0; transform: scale(0.95) translateY(20px); }
  to { opacity: 1; transform: scale(1) translateY(0); }
}

.terminal-header {
  background: rgba(0, 0, 0, 0.6);
  padding: var(--space-3) var(--space-5);
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-bottom: 1px solid var(--border);
}

.terminal-dots {
  display: flex;
  gap: 8px;
}

.dot {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  opacity: 0.6;
}

.dot-red { background: #ff5f56; }
.dot-yellow { background: #ffbd2e; }
.dot-green { background: #27c93f; }

.terminal-title {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--text-muted);
  letter-spacing: 2px;
}

.tactical-progress {
  padding: var(--space-5) var(--space-7);
  font-family: var(--mono);
  font-size: 11px;
  color: var(--cyan);
  display: flex;
  align-items: center;
  gap: var(--space-4);
  background: rgba(0, 242, 254, 0.02);
}

.progress-mono {
  letter-spacing: -2px;
  font-weight: 700;
}

.wizard-body {
  padding: var(--space-7);
  min-height: 420px;
}

.wizard-step-title {
  font-family: var(--heading);
  font-size: 14px;
  font-weight: 700;
  text-transform: uppercase;
  color: var(--cyan);
  margin-bottom: var(--space-6);
  display: flex;
  align-items: center;
  gap: 12px;
}

.wizard-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-4);
}

.wizard-panel {
  background: var(--bg-card);
  border: 1px solid var(--border);
  padding: var(--space-5);
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: all 0.2s ease;
  position: relative;
}

.wizard-panel:hover, .wizard-panel.active {
  background: var(--bg-hi);
  border-color: var(--cyan-dim);
  box-shadow: 0 0 15px var(--cyan-glass);
}

.wizard-panel.active {
  border-color: var(--cyan);
  background: rgba(0, 242, 254, 0.05);
}

.wizard-footer {
  padding: var(--space-5) var(--space-7);
  border-top: 1px solid var(--border);
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: rgba(0, 0, 0, 0.4);
}

.footer-key {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--text-muted);
}

.footer-key span {
  color: var(--cyan);
  font-weight: 700;
}

/* ── Markdown body ───────────────────────────────────────────────────────── */
.markdown-body {
  word-break: break-word;
  overflow-wrap: break-word;
}

.markdown-body pre {
  white-space: pre-wrap;
  word-break: break-word;
  overflow-x: auto;
  max-width: 100%;
}

/* ── Terminal boot sequence lines ────────────────────────────────────────── */
.boot-line {
  font-family: var(--mono);
  font-size: 13px;
  color: rgba(255, 255, 255, 0.6);
  display: flex;
  justify-content: space-between;
  align-items: center;
  opacity: 0;
  animation: fadeInUp 0.4s ease forwards;
  margin-bottom: 8px;
}

.boot-ok {
  color: var(--green);
  font-family: var(--mono);
  font-size: 12px;
}

.boot-spinner {
  color: var(--cyan);
  animation: blink 0.5s infinite;
}

/* ── Scroll-reveal base ──────────────────────────────────────────────────── */
.reveal {
  opacity: 0;
  transform: translateY(40px);
  transition: opacity 0.6s cubic-bezier(0.16, 1, 0.3, 1),
    transform 0.6s cubic-bezier(0.16, 1, 0.3, 1);
}

.reveal.revealed {
  opacity: 1;
  transform: translateY(0);
}

/* ═══════════════════════════════════════════════════════════════════════════
   RESPONSIVE
   ═══════════════════════════════════════════════════════════════════════════ */
@media (max-width: 768px) {
  :root {
    font-size: 14px;
  }

  .custom-cursor {
    display: none;
  }
}

@media (max-width: 400px) {
  :root {
    font-size: 13px;
  }
}

/* ── Touch devices: hide custom cursor ───────────────────────────────────── */
@media (pointer: coarse) {
  * {
    cursor: auto !important;
  }

  .custom-cursor {
    display: none !important;
  }
}

/* ═══════════════════════════════════════════════════════════════════════════
   REDUCED MOTION — accessibility
   ═══════════════════════════════════════════════════════════════════════════ */
@media (prefers-reduced-motion: reduce) {

  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }

  .cyber-grid-lg,
  .cyber-grid-sm,
  .scan-overlay,
  .marquee-track {
    animation: none !important;
  }
}
